export enum Schema {
  INTEGER = 'integer',
  DECIMAL = 'decimal',
  // ATTACHMENT = 'attachment',
  BOOLEAN = 'boolean',
  // EMAIL = 'email',
  PASSWORD = 'password',
  // IMAGE = 'image',
  // JSON = 'json',
  TEXT = 'text',
  DATETIME = 'datetime',
  // TIMESTAMP = 'timestamp',
  // ENUM = 'enum',
}
